/**
 * Write a program to check if a given number is even or odd.
 *
 */

// if number conpletely divides 2 then even eles odd
const evenOdd = (number) => (number % 2 === 0 ? "even" : "odd");

console.log(evenOdd(82));
