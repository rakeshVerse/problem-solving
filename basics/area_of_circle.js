/**
 * Write a program to calculate the area of a circle given its radius.
 *
 * Area of Circle (A) = pi * r^2
 * where,
 *  pi = 3.14159
 *  r = radius
 */

const area = (radius) => 3.14159 * radius ** 2;

console.log(area(5));
